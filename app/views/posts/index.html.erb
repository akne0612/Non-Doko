
<%= render 'posts/map_box' %>

<div class="container py-4">
  <div class="row g-4 justify-content-center">
    <% @posts.each do |post| %>
      <div class="col-12 col-sm-6 col-md-4 col-lg-3 d-flex">
        <div class="card h-100 text-center w-100">
          <%= image_tag(
                post.image.attached? ? post.image.variant(resize_to_fill: [600, 400]) : "no_image.png",
                class: "card-img-top"
              ) %>

          <div class="card-body d-flex flex-column">
            <h5 class="card-title mb-2"><%= post.title %></h5>

            <div class="text-muted small mb-3">
              ❤ <%= post.likes_count || post.likes.size %>
              &nbsp; / &nbsp;
              💬 <%= post.comments.size %>
            </div>

            <div class="mt-auto">
              <%= render "posts/like_button", post: post %>
              <%= link_to "詳細", post_path(post), class: "btn btn-sm btn-primary ms-2" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

 
</div>

<%= render 'posts/map_js' %>