<div class="card p-3 mb-3">
  <h2 class="h6 mb-2">場所</h2>

  <div id="post-map"
       style="width:100%;height:360px;"
       data-lat="<%= @post.latitude %>"
       data-lng="<%= @post.longitude %>"
       data-title="<%= j(@post.title.presence || @post.try(:store_name) || '投稿場所') %>"
       data-address="<%= j(@post.try(:address).to_s) %>"
       data-icon-url="<%= asset_path('beer-pin.png') %>"></div>

  <small class="text-muted">※ ピンをタップするとタイトルが表示されます</small>
</div>